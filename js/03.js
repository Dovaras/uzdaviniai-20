const h1Query = document.querySelector('h1');
h1Query.classList.add('simba');

const liQuery = document.querySelectorAll('li');
for (let i = 0; i < liQuery.length; i++) {
    liQuery[i].classList.add('darzove');
}